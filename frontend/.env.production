# Production build (Vercel): leave empty so the app calls /api/v1/* on its own
# origin. Vercel's rewrite in vercel.json proxies those to the backend droplet,
# which keeps the browser on HTTPS and avoids mixed-content blocking.
VITE_API_BASE=
